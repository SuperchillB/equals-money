/**
 * This is where we can store all our base query keys, each of which is based on a feature.
 * If we were to implement a new feature for 'todos', we would have to create a base query
 * key called 'todos'.
 */

export const contactsBaseQueryKey = 'contacts';
